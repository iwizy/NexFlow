specVersion: "0.1"
kind: Project
metadata:
  project: cli-fixture
project:
  id: cli-fixture
  displayName: CLI Fixture
  description: Contains an intentionally rejected escaping source hint.
manifests:
  agents: ../outside.yaml
